#include<iostream>
#include<string>
using namespace std;

int main() {
	string s;
	int space;
	while (getline(cin, s)) {
		space = 0;
		for (int i = 1; i < s.length(); i++) {
			if (s[i] != ' '&&s[i - 1] == ' ')
				space++;
		}
		cout << space + 1 << endl;
	}
}
