#include <stdio.h>
#include <stdlib.h>
#include <string.h>
char a[100][200]; //定义最大100行，每行200个字符的字符串数组
int i,j;
void sort()
{
        char temp[200];
        for(j=0;j<n;j++)
        for(i=0;i<n-j-1;i++)
        {
                if(strcmp(a[i],a[i+1])>0)
                {       
                strcpy(temp,a[i]);
                strcpy(a[i],a[i+1]);
                strcpy(a[i+1],temp);
                }
        }
}
void main()
{
scanf("%d",&n);//输入参与排序的字符串个数
getchar(); //清除回车符
for(i=0;i<n;i++)
        gets(a[i]);
sort();//调用排序函数
for(i=0;i<n;i++)
        puts(a[i]);
} 