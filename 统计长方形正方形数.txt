#include<iostream>
#include<algorithm>
using namespace std;

int main()
{
	long long n, m;
	while (cin >> n >> m)
	{
		long  s1, s2;
		if (m > n)swap(m, n);
		s1 = m * (m + 1)*(3 * n + 1 - m) / 6;
		s2 = n * m*(n + 1)*(m + 1) / 4;
		cout << s1 << " " << s2 - s1 << endl;
	}
	return 0;
}