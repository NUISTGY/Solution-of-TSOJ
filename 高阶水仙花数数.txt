#include <stdio.h>
double power(double x,int m);
int main()
{
int n,i,a,c,sum;
while (scanf("%d",&n)!=EOF){
for(i=(int)power(10.0,n-1);i<(int)power(10.0,n);i++){
a=i;
sum=0;
while(a){
c=a%10;
sum=sum+(int)power(c*1.0,n);
a=a/10;
}
if(sum==i){
printf("%d\n",i);
}
}
}
return 0;
}
double power(double x,int m)
{
   double h=1.0;
   int i;
   for(i=1;i<=m;i++)
   h*=x;
   return h;	
}